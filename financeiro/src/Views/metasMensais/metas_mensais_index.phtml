<?php use MF\Helpers\NumbersHelper; ?>

<div class="container">
    <div class="mt-2 mb-2">
        <div class="card p-2">
            <div class="text-center">Metas Mensais</div>
        </div>
    </div>

    <div class="row my-2">
        <div class="col-12 col-sm-4">
            <div class="card">
                <div class="card-body">
                    <label for="selectProprietario" class="form-label">Filtrar Proprietário</label>
                    <select class="form-select" id="idProprietario" name="idProprietario">
                        <?php
                            foreach ($lista_proprietarios as $prop):
                                $x = $prop['idProprietario'];
                                $n = $prop['proprietario'];
                        ?>
                            <option value="<?= $x ?>"> <?= $n; ?></option>
                        <?php endforeach; ?>
                    </select>
                </div>
            </div>
        </div>
    </div>

    <div class="row" id="container-metas">
        <?php foreach ($lista_mm as $v): ?>
            <div class="col-12 col-md-6 col-lg-4 mb-4">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <h6 class="card-title mb-0"><?= $v['mesAno']; ?></h6>
                    </div>
                    <div class="card-body">
                        <div class="mb-3 pb-2 border-bottom">
                            <div class="fw-bold text-secondary">Total de Receitas</div>
                            <div class="text-primary fw-bold">R$ <?= NumbersHelper::formatUStoBR($v['totalReceitas']); ?></div>
                        </div>
                        <div class="mb-3 pb-2 border-bottom">
                            <div class="fw-bold text-secondary">Valor a Economizar</div>
                            <div class="text-success fw-bold">R$ <?= NumbersHelper::formatUStoBR($v['vlrEconomia']); ?></div>
                        </div>
                        <div class="mb-3 pb-2 border-bottom">
                            <div class="fw-bold text-secondary">Percentual de Economia</div>
                            <div><?= $v['totalReceitas'] > 0 ? NumbersHelper::formatUStoBR($v['vlrEconomia'] / $v['totalReceitas']) : '0,00'; ?>%</div>
                        </div>
                        <div class="mb-3">
                            <div class="fw-bold text-secondary">Proprietário</div>
                            <div><?= $v['proprietario']; ?></div>
                        </div>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</div>