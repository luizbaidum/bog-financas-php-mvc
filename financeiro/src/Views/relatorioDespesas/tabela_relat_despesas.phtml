
<?php
    use MF\Helpers\DateHelper;
    use MF\Helpers\NumbersHelper;
    $total = 0;
    $total_linhas = is_array($relatorio) ? count($relatorio) : 0;
?>

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white border-bottom-0 pt-4 pb-2">
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
            <div class="d-flex align-items-center">
                <div>
                    <h6 class="card-title mb-0 fw-bold text-dark">Relatorio de Despesas</h6>
                    <p class="text-muted small mb-0">Acompanhe a disposição das despesas</p>
                </div>
            </div>
        </div>
    </div>

    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                    <tr class="text-secondary">
                        <th class="ps-4 py-3 border-0 fw-semibold">idMovimento</th>
                        <th class="py-3 border-0 fw-semibold">Data</th>
                        <th class="py-3 border-0 fw-semibold">Descrição</th>
                        <th class="py-3 border-0 fw-semibold">Despesa</th>
                        <th class="py-3 border-0 fw-semibold text-end">Valor</th>
                        <th class="py-3 border-0 fw-semibold">Proprietário</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($relatorio)): ?>
                        <tr>
                            <td colspan="6" class="text-center text-muted py-5">
                                <i class="bi bi-inbox text-muted fs-1 d-block mb-3"></i>
                                <p class="text-muted mb-0">Nenhum registro encontrado.</p>
                            </td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($relatorio as $value):
                            $total += $value['valor'];
                        ?>
                            <tr class="border-bottom">
                                <td class="ps-4 py-3">
                                    <?= $value['idMovimento']; ?>
                                </td>
                                <td class="py-3">
                                    <?= DateHelper::convertUStoBR($value['dataMovimento']); ?>
                                </td>
                                <td class="py-3">
                                    <?= $value['nomeMovimento']; ?>
                                </td>
                                <td>
                                    <?= $value['categoria'] . ' (' . $value['regularidade'] . ')'; ?>
                                </td>
                                <td class="py-3 text-end">R$ <?= NumbersHelper::formatUStoBR($value['valor']); ?></td>
                                <td class="py-3"><?= $value['proprietario']; ?></td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
                <tfoot>
                    <tr class="table-dark">
                        <td class="ps-4">Total de registros: <?= $total_linhas; ?></td>
                        <td colspan="3" class="text-end">Total</td>
                        <td class="text-end">R$ <?= NumbersHelper::formatUStoBR($total); ?></td>
                        <td class="pe-4"></td>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>

    <?php if (! empty($relatorio)): ?>
        <div class="card-footer bg-white border-top-0 pt-0 pb-3 px-4">
            <div class="d-flex justify-content-between align-items-center">
                <small class="text-muted">
                    <i class="bi bi-info-circle me-1"></i>
                    <?= $total_linhas; ?> registro(s) no relatório
                </small>
            </div>
        </div>
    <?php endif; ?>
</div>