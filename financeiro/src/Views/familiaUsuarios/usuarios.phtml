<div class="container">
    <div class="mt-2 mb-2">
        <div class="card p-2">
            <div class="text-center">
                <?= $title; ?>
            </div>
        </div>
    </div>

    <?php if ($is_gestor): ?>
        <div class="card mt-2 mb-2 formulario-padrao-card">
            <div class="card-header bg-dark text-white">
                <button class="<?= isset($usuario_editar) ? 'btn btn-sm nav-link' : 'btn btn-sm nav-link collapsed'; ?>" type="button" data-bs-toggle="collapse" data-bs-target="#collapseCadUsuario" aria-expanded="<?= isset($usuario_editar) ? 'true' : 'false'; ?>" aria-controls="collapseCadUsuario">
                    <h6 class="card-title mb-0">Cadastrar Usuário</h6>
                </button>
            </div>
            <div class="collapse <?= isset($usuario_editar) ? 'show' : ''; ?>" id="collapseCadUsuario">
                <div class="card-body p-1">
                    <form data-action="<?= $action; ?>" id="idform-usuario" class="submit-form-crud-ajax">

                        <?php if (isset($usuario_editar)): ?>
                            <input type="hidden" name="idUsuario" value="<?= $usuario_editar['idUsuario']; ?>">
                        <?php endif; ?>

                        <div class="form-group formulario-padrao-section">
                            <div class="row g-2">
                                <div class="col-md-3">
                                    <label for="idNome" class="form-label formulario-padrao-label">Nome</label>
                                    <input type="text" class="form-control formulario-padrao-input form-control-sm" id="idNome" name="nome" required 
                                        placeholder="Nome do usuário" value="<?= isset($usuario_editar) ? $usuario_editar['nome'] : ''; ?>">
                                </div>
                                <div class="col-md-3">
                                    <label for="idLogin" class="form-label formulario-padrao-label">Login</label>
                                    <input type="text" class="form-control formulario-padrao-input form-control-sm" id="idLogin" name="login" required 
                                        placeholder="Login" value="<?= isset($usuario_editar) ? $usuario_editar['login'] : ''; ?>">
                                </div>
                                <div class="col-md-3">
                                    <label for="idSenha" class="form-label formulario-padrao-label">Senha</label>
                                    <input type="password" class="form-control formulario-padrao-input form-control-sm" id="idSenha" name="senha" <?= isset($usuario_editar) ? '' : 'required'; ?>
                                        placeholder="Senha segura">
                                </div>
                                <div class="col-md-3">
                                    <label for="idConfirmaSenha" class="form-label formulario-padrao-label">Confirmar</label>
                                    <input type="password" class="form-control formulario-padrao-input form-control-sm" id="idConfirmaSenha" name="confirmaSenha" <?= isset($usuario_editar) ? '' : 'required'; ?>
                                        placeholder="Repita a senha">
                                </div>
                            </div>
                        </div>
                        <button id="salvar" type="submit" class="btn btn-success btn-sm m-1">Salvar</button>
                    </form>
                </div>
            </div>
        </div>
    <?php endif; ?>

    <div class="row">
        <div class="col-3">
            <div class="card mb-3">
                <div class="card-header bg-light">
                    <h5 class="card-title mb-0">Família <?= $familia; ?></h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">Total de Membros: <?= count($lista_usuarios); ?></li>
                        <li class="list-group-item">Gestores: <?= array_reduce($lista_usuarios, function($carry, $item) { return $carry + ($item['gestor'] == 'T' ? 1 : 0); }, 0); ?></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="col-9">
            <div class="table-responsive">
                <table class="table table-sm table-hover">
                    <thead class="table-light">
                        <tr>
                            <th scope="col" class="text-center" style="width: 10%;">ID</th>
                            <th scope="col" style="width: 50%;">Nome</th>
                            <th scope="col" class="text-center" style="width: 20%;">Tipo</th>
                            <?php if ($is_gestor): ?>
                                <th scope="col" style="width: 10%;">Editar</th>
                            <?php endif; ?>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($lista_usuarios as $usuario): ?>
                            <?php if ($usuario['gestor'] == 'T'): ?>
                                <tr class="bg-primary bg-opacity-10">
                                    <th scope="row" class="text-center fw-bold"><?= $usuario['idUsuario']; ?></th>
                                    <td class="fw-bold">
                                        <i class="bi bi-person-badge text-primary me-2"></i>
                                        <?= $usuario['nome']; ?>
                                    </td>
                                    <td class="text-center">
                                        <span class="badge bg-primary rounded-pill">Gestor</span>
                                    </td>
                                    <?php if ($is_gestor): ?>
                                        <td>
                                            <a href="<?= $edit['route'] . '&idUsuario=' . $usuario['idUsuario']; ?>" class="link-offset-2 link-underline link-underline-opacity-0">
                                                <button title="Editar" class="btn btn-sm btn-outline-dark">
                                                    <i>✎</i>
                                                </button>
                                            </a>
                                        </td>
                                    <?php endif; ?>
                                </tr>
                            <?php else: ?>
                                <tr>
                                    <th scope="row" class="text-center"><?= $usuario['idUsuario']; ?></th>
                                    <td>
                                        <i class="bi bi-person text-muted me-2"></i>
                                        <?= $usuario['nome']; ?>
                                    </td>
                                    <td class="text-center">
                                        <span class="badge bg-secondary rounded-pill">Membro</span>
                                    </td>
                                    <?php if ($is_gestor): ?>
                                        <td>
                                            <a href="<?= $edit['route'] . '&idUsuario=' . $usuario['idUsuario']; ?>" class="link-offset-2 link-underline link-underline-opacity-0">
                                                <button title="Editar" class="btn btn-sm btn-outline-dark">
                                                    <i>✎</i>
                                                </button>
                                            </a>
                                        </td>
                                    <?php endif; ?>
                                </tr>
                            <?php endif; ?>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>