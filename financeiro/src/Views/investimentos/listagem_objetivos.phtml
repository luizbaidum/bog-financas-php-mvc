<?php use MF\Helpers\NumbersHelper; ?>

<div class="card border-0 shadow-sm mt-4">
    <div class="card-header bg-white border-bottom-0 pt-4 pb-2">
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
            <div class="d-flex align-items-center">
                <div>
                    <h6 class="card-title mb-0 fw-bold text-dark">Seus Objetivos</h6>
                    <p class="text-muted small mb-0">Acompanhe o progresso dos seus investimentos</p>
                </div>
            </div>
            <div class="d-flex gap-2">
                <div class="badge bg-success bg-opacity-10 text-success px-3 py-2 rounded-pill">
                    <i class="bi bi-check-circle-fill me-1 small"></i>
                    <?= count(array_filter($lista_obj ?? [], function($item) { return $item['finalizado'] == 'T'; })); ?> Concluídos
                </div>
                <div class="badge bg-warning bg-opacity-10 text-warning px-3 py-2 rounded-pill">
                    <i class="bi bi-hourglass-split me-1 small"></i>
                    <?= count(array_filter($lista_obj ?? [], function($item) { return $item['finalizado'] == 'F'; })); ?> Em andamento
                </div>
            </div>
        </div>
    </div>

    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                    <tr class="text-secondary">
                        <th class="ps-4 py-3 border-0 fw-semibold">Objetivo</th>
                        <th class="py-3 border-0 fw-semibold text-end">Meta</th>
                        <th class="py-3 border-0 fw-semibold text-end">Saldo Atual</th>
                        <th class="py-3 border-0 fw-semibold text-center">Progresso</th>
                        <th class="py-3 border-0 fw-semibold text-center">% Alocado</th>
                        <th class="py-3 border-0 fw-semibold text-center">Status</th>
                        <th class="pe-4 py-3 border-0 text-center" width="50">
                            <input type="checkbox" class="form-check-input" id="selectAll" onclick="toggleAllCheckboxes(this)">
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($lista_obj as $k => $v): 
                        $progresso = ($v['saldoAtual'] / $v['vlrObj']) * 100;
                        $progresso = min(100, round($progresso, 1));
                        $corProgresso = $progresso < 30 ? 'bg-danger' : ($progresso < 70 ? 'bg-warning' : 'bg-success');
                    ?>
                        <tr class="border-bottom">
                            <td class="ps-4 py-3">
                                <div class="d-flex align-items-center">
                                    <div>
                                        <div class="fw-semibold text-dark"><?= $v['idObj'] . ' - ' . $v['nomeObj']; ?></div>
                                        <small class="text-muted">Conta #<?= $v['idContaInvest']; ?></small>
                                    </div>
                                </div>
                            </td>
                            <td class="text-end py-3">
                                <span class="fw-semibold">R$ <?= NumbersHelper::formatUStoBR($v['vlrObj']); ?></span>
                            </td>
                            <td class="text-end py-3">
                                <span class="text-success fw-semibold">R$ <?= NumbersHelper::formatUStoBR($v['saldoAtual']); ?></span>
                            </td>
                            <td class="py-3" style="min-width: 180px;">
                                <div class="d-flex align-items-center gap-2">
                                    <div class="flex-grow-1">
                                        <div class="progress" style="height: 8px;">
                                            <div class="<?= $corProgresso; ?>"
                                                 role="progressbar"
                                                 style="width: <?= $progresso; ?>%"
                                                 aria-valuenow="<?= $progresso; ?>"
                                                 aria-valuemin="0"
                                                 aria-valuemax="100">
                                            </div>
                                        </div>
                                    </div>
                                    <small class="text-muted fw-semibold" style="min-width: 45px;"><?= $progresso; ?>%</small>
                                </div>
                            </td>
                            <td class="text-center py-3">
                                <span class="badge bg-info bg-opacity-10 text-info px-3 py-2 rounded-pill">
                                    <?= NumbersHelper::formatUStoBR($v['percentObjContaInvest']); ?>%
                                </span>
                            </td>
                            <td class="text-center py-3">
                                <select data-url="<?= $extra_url . $v['idObj']; ?>" 
                                        class="form-select form-select-sm w-auto d-inline-block" 
                                        id="status-<?= $k; ?>" 
                                        name="finalizado[]" 
                                        onchange="atualizarStatus(this)"
                                        style="background-color: <?= $v['finalizado'] == 'T' ? '#d1e7dd' : '#fff3cd'; ?>">
                                    <option value="F" <?= $v['finalizado'] == 'F' ? 'selected' : ''; ?> class="text-warning">
                                        🔄 Em andamento
                                    </option>
                                    <option value="T" <?= $v['finalizado'] == 'T' ? 'selected' : ''; ?> class="text-success">
                                        ✓ Concluído
                                    </option>
                                </select>
                            </td>
                            <td class="pe-4 py-3 text-center">
                                <input type="checkbox" name="selectedData[]" value="<?= $v['idObj']; ?>" class="form-check-input data-checkbox">
                            </td>
                        </tr>
                    <?php endforeach; ?>

                    <?php if (empty($lista_obj)): ?>
                        <tr>
                            <td colspan="7" class="text-center py-5">
                                <i class="bi bi-inbox text-muted fs-1 d-block mb-3"></i>
                                <p class="text-muted mb-0">Nenhum objetivo cadastrado ainda</p>
                                <button class="btn btn-sm btn-primary mt-3" onclick="document.querySelector('#idNomeObj')?.focus()">
                                    <i class="bi bi-plus-circle me-1"></i>Criar primeiro objetivo
                                </button>
                            </td>
                        </tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>

    <?php if (! empty($lista_obj)): ?>
    <div class="card-footer bg-white border-top-0 pt-0 pb-3 px-4">
        <div class="d-flex justify-content-between align-items-center">
            <small class="text-muted">
                <i class="bi bi-info-circle me-1"></i>
                <?= count($lista_obj); ?> objetivo(s) cadastrado(s)
            </small>
        </div>
    </div>
    <?php endif; ?>
</div>