<div class="card border-0 shadow-sm mt-4">
    <div class="card-header bg-white border-bottom-0 pt-4 pb-2">
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
            <div class="d-flex align-items-center">
                <div>
                    <h6 class="card-title mb-0 fw-bold text-dark">Lista de Investimentos</h6>
                    <p class="text-muted small mb-0">Acompanhe todas as suas contas de investimento</p>
                </div>
            </div>
        </div>
    </div>

    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                    <tr class="text-secondary">
                        <th class="ps-4 py-3 border-0 fw-semibold">Banco</th>
                        <th class="py-3 border-0 fw-semibold">Título</th>
                        <th class="py-3 border-0 fw-semibold">Proprietário</th>
                        <th class="pe-4 py-3 border-0 fw-semibold text-center">Status</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($todas_contas as $k => $v): ?>
                        <tr class="border-bottom">
                            <td class="ps-4 py-3">
                                <div class="fw-semibold text-dark"><?= $v['nomeBanco']; ?></div>
                            </td>
                            <td class="py-3">
                                <span><?= $v['tituloInvest']; ?></span>
                            </td>
                            <td class="py-3">
                                <span><?= $v['proprietario']; ?></span>
                            </td>
                            <td class="pe-4 py-3 text-center">
                                <select data-url="<?= $extra_url . $v['idContaInvest']; ?>" 
                                        class="form-select form-select-sm w-auto d-inline-block" 
                                        id="status-<?= $k; ?>" 
                                        name="status[]" 
                                        onchange="atualizarStatus(this)"
                                        style="background-color: <?= $v['status'] == '1' ? '#d1e7dd' : '#f8d7da'; ?>">
                                    <option value="1" <?= $v['status'] == '1' ? 'selected' : ''; ?> class="text-success">
                                        ✓ Ativo
                                    </option>
                                    <option value="0" <?= $v['status'] == '0' ? 'selected' : ''; ?> class="text-danger">
                                        ✗ Inativo
                                    </option>
                                </select>
                            </td>
                        </tr>
                    <?php endforeach; ?>

                    <?php if (empty($todas_contas)): ?>
                        <tr>
                            <td colspan="4" class="text-center py-5">
                                <i class="bi bi-inbox text-muted fs-1 d-block mb-3"></i>
                                <p class="text-muted mb-0">Nenhum investimento cadastrado ainda</p>
                                <button class="btn btn-sm btn-primary mt-3" onclick="document.querySelector('#idNomeBanco')?.focus()">
                                    <i class="bi bi-plus-circle me-1"></i>Criar primeiro investimento
                                </button>
                            </td>
                        </tr>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>

    <?php if (! empty($todas_contas)): ?>
        <div class="card-footer bg-white border-top-0 pt-0 pb-3 px-4">
            <div class="d-flex justify-content-between align-items-center">
                <small class="text-muted">
                    <i class="bi bi-info-circle me-1"></i>
                    <?= count($todas_contas); ?> investimento(s) cadastrado(s)
                </small>
            </div>
        </div>
    <?php endif; ?>
</div>