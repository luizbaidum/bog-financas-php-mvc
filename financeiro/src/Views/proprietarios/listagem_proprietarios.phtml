<div class="card">
    <div class="card-header bg-dark text-white">
        <h6 class="card-title mb-0">Lista</h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-sm table-hover">
                <thead class="table-primary">
                    <tr>
                        <th>Proprietário</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($todos_prop as $k => $v): ?>
                        <tr>
                            <td><?= $v['proprietario']; ?></td>
                            <td>
                                <select data-url="<?= $extra_url . $v['idProprietario']; ?>" class="form-control" id="status-<?= $k; ?>" name="status[]" onchange="atualizarStatus(this)">
                                    <option value="1" <?= $v['status'] == '1' ? ' selected ' : ''; ?>>Ativo</option>
                                    <option value="0" <?= $v['status'] == '0' ? ' selected ' : ''; ?>>Inativo</option>
                                </select>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>