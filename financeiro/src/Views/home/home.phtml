<?php 
    use MF\Helpers\DateHelper;
    use MF\Helpers\NumbersHelper;

    $acumulado = 0;
    $resultado_invests = 0;
?>

<div class="container py-3">
    <?php if ($exibir_cards): ?>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-3 mb-4 <?= $card['div']; ?>">
            <?php require ('cards_resumo.php'); ?>
        </div>
    <?php endif;  ?>

    <div class="card shadow-sm border-0 mt-1">
        <div class="card-header d-flex justify-content-between align-items-center py-2 bg-white">
            <span class="fw-semibold"><i class="bi bi-graph-up me-1"></i>Movimentos</span>
            <a href="movimentos" class="link-offset-2 link-underline link-underline-opacity-0">
                <button class="btn btn-sm btn-outline-primary">Novo</button>
            </a>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-striped table-hover table-sm mb-0 align-middle">
                    <thead>
                        <tr>
                            <th class="text-uppercase small text-muted">id</th>
                            <td></td>
                            <th class="text-uppercase small text-muted">Movimento</th>
                            <th class="text-uppercase small text-muted">Categoria</th>
                            <th class="text-uppercase small text-muted">Valor</th>
                            <th class="text-uppercase small text-muted">Prop.</th>
                            <th class="text-uppercase small text-muted text-center" style="width: 5%;">Editar</th>
                            <th class="text-center" width="50">
                                <input type="checkbox" class="form-check-input" id="selectAll" onclick="toggleAllCheckboxes(this)">
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php if (empty($prop)): ?>
                            <tr><td colspan="8" class="text-center text-muted py-4">Sem movimentos para o período/filtro.</td></tr>
                        <?php else: ?>
                            <?php foreach ($movimentos_agrupados as $data => $lancamentosDoDia): ?>
                                <tr class="bg-light bg-opacity-75">
                                    <td colspan="8" class="px-3 py-1 text-uppercase small fw-semibold text-white bg-dark">
                                        <?= $data ?>
                                    </td>
                                </tr>
                                <?php foreach ($lancamentosDoDia as $mov): ?>
                                    <tr>
                                        <td><?= $mov['idMovimento']; ?></td>
                                        <td>
                                            <button title="Detalhes" 
                                                    class="btn btn-sm btn-outline-primary render-ajax" 
                                                    data-url="<?= $url_detail . $mov['idMovimento']; ?>" 
                                                    data-div=""
                                                    data-modal="true">
                                                &#128489;
                                            </button>
                                        </td>
                                        <td>
                                            <?= $mov['nomeMovimento']; ?>
                                        </td>
                                        <td><?= $mov['categoria']; ?></td>
                                        <td class="fw-semibold">R$ <?= NumbersHelper::formatUStoBR($mov['valor']); ?></td>
                                        <td><?= $prop[$mov['idProprietario']]; ?></td>
                                        <td>
                                            <a href="<?= $url_edit . $mov['idMovimento']; ?>" class="link-offset-2 link-underline link-underline-opacity-0">
                                                <button title="Editar" class="btn btn-sm btn-outline-dark">
                                                    <i>&#9998;</i>
                                                </button>
                                            </a>
                                        </td>
                                        <td class="text-center" width="50">
                                            <input class="form-check-input data-checkbox" type="checkbox" name="selectedData[]" value="<?= $mov['idMovimento']; ?>">
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </tbody>
                    <tfoot>
                        <tr class="table-info">
                            <td></td>
                            <td></td>
                            <td colspan="2" class="text-end fw-semibold">Resultado (Rec. - Des. - Apli.)</td>
                            <td class="fw-bold">R$ <?= NumbersHelper::formatUStoBR($resultado); ?></td>
                            <td>
                                <?php foreach ($result_por_prop as $id_prop => $valor): ?>
                                    <small class="text-muted d-block"><?= $prop[$id_prop]; ?>: <?= NumbersHelper::formatUStoBR($valor); ?></small>
                                <?php endforeach; ?>
                            </td>
                            <td colspan="2"></td>
                        </tr>
                        <?php
                            if (! empty($saldos_anteriores)):
                                $acumulado = $resultado;
                        ?>
                            <?php
                                foreach ($saldos_anteriores as $value):
                                    $acumulado += $value['valor'];
                            ?>
                                <tr class="table-info">
                                    <td></td>
                                    <td colspan="2" class="text-end">Resultado mês <?= $value['MES']; ?>:</td>
                                    <td class="fw-semibold">R$ <?= NumbersHelper::formatUStoBR($value['valor']); ?></td>
                                    <td colspan="4"></td>
                                </tr>
                            <?php endforeach; ?>
                                <tr class="table-info">
                                    <td></td>
                                    <td colspan="2" class="text-end fw-semibold">Acumulado</td>
                                    <td class="fw-bold">R$ <?= NumbersHelper::formatUStoBR($acumulado); ?></td>
                                    <td colspan="4"></td>
                                </tr>
                        <?php endif; ?>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>

    <?php if ($exibir_resumo_investimentos): ?>
        <div class="card shadow-sm border-0 mt-3">
            <div class="card-header d-flex justify-content-between align-items-center py-2 bg-white">
                <span class="fw-semibold"><i class="bi bi-graph-up me-1"></i>Investimentos</span>
                <div>
                    <a href="contas-investimentos-index" class="link-offset-2 link-underline link-underline-opacity-0">
                        <button class="btn btn-sm btn-outline-primary">Novo</button>
                    </a>
                </div>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped table-hover table-sm mb-0 align-middle">
                        <thead>
                            <tr>
                                <th class="text-uppercase small text-muted">Data</th>
                                <th class="text-uppercase small text-muted">Tipo</th>
                                <th class="text-uppercase small text-muted">Conta</th>
                                <th class="text-uppercase small text-muted">Valor</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php 
                                foreach ($mov_investimentos as $mov): 
                                    $resultado_invests += $mov['valor'];
                            ?>
                                <tr>
                                    <td><?= DateHelper::convertUStoBR($mov['dataMovimento']); ?></td>
                                    <td><?= explode(' ', $mov['categoria'])[0]; ?></td>
                                    <td><?= $mov['invest']; ?></td>
                                    <td class="fw-semibold">R$ <?= NumbersHelper::formatUStoBR($mov['valor']); ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                        <tfoot>
                            <tr class="table-info">
                                <td colspan="3" class="text-end fw-semibold">Acumulado</td>
                                <td class="fw-bold">R$ <?= NumbersHelper::formatUStoBR($resultado_invests); ?></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    <?php endif; ?>
</div>