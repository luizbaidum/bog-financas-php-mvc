

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <div class="text-center mb-4">
                        <h1 class="h4 mb-1">Bog Finanças - Bem-vindo(a)!</h1>
                        <p class="text-muted mb-0">Vamos configurar sua conta.</p>
                    </div>

                    <div class="alert alert-info" role="alert">
                        <h4 class="alert-heading h6 mb-2">Informações importantes</h4>
                        <p class="mb-1">Como é seu primeiro acesso, você precisará criar um login e senha para acessar o sistema.</p>
                        <p class="mb-0">Após essa etapa, você deverá definir sua família no sistema.</p>
                    </div>

                    <form
                        class="submit-form-crud-ajax-pa"
                        method="POST"
                        name="form-crud"
                        id="idform-crud"
                        data-action="<?= $action; ?>"
                        data-redirect="<?= $redirect; ?>"
                    >
                        <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="nome" id="name" placeholder="Nome" required>
                            <label for="name">Nome</label>
                        </div>

                        <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="login" id="login" placeholder="Login" required>
                            <label for="login">Login</label>
                        </div>

                        <div class="form-floating mb-3">
                            <input type="password" class="form-control" name="senha" id="password" placeholder="Senha" required>
                            <label for="password">Senha</label>
                        </div>

                        <div class="form-floating mb-2">
                            <input type="password" class="form-control" name="confirmaSenha" id="confirmPassword" placeholder="Repita a senha" required>
                            <label for="confirmPassword">Repita a senha</label>
                        </div>

                        <div class="mb-3 d-flex justify-content-between align-items-center">
                            <div>
                                <small class="password-match text-success">✔ As senhas coincidem</small>
                                <small class="password-mismatch text-danger">✖ As senhas não coincidem</small>
                            </div>
                        </div>

                        <div class="form-floating mb-4">
                            <input type="text" class="form-control" name="hash" id="hash" placeholder="Hash única" required>
                            <label for="hash">Hash única</label>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg" id="submitBtn">Continuar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>